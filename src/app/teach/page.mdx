export const metadata = {title: '教案'}

import Navigator from '../../Navigator'
import {metadata as objective} from './objective/page.mdx'

<Navigator>{{objective: objective.title}}</Navigator>

# 有效流

你好，世界！
