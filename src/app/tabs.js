import {metadata as teachMetadata} from './teach/page.mdx'
import {metadata as learnMetadata} from './learn/page.mdx'

const tabs = {teach: teachMetadata.title, learn: learnMetadata.title}
export default tabs
